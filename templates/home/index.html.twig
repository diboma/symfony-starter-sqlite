{% extends 'layouts/base.html.twig' %}

{% block title %}Home{% endblock %}

{% block body %}

<style>
  .example-wrapper {
    margin: 1em auto;
    max-width: 800px;
    width: 95%;
    font: 18px/1.5 sans-serif;
  }

  .example-wrapper code {
    background: #F5F5F5;
    padding: 2px 6px;
  }
</style>

<div class="example-wrapper">

    <h1>Hello {{ controller_name }}! ✅</h1>

    This friendly message is coming from:

    <ul>
        <li>Your controller at <code>C:/Users/Dirk/Desktop/sqlite-symfony-starter/src/Controller/HomeController.php</code>
        </li>
        <li>Your template at <code>C:/Users/Dirk/Desktop/sqlite-symfony-starter/templates/home/index.html.twig</code></li>
    </ul>

    <h2 class="mt-4">Test autocomplete</h2>

    {# {{ form_start(form_autocomplete, {
            attr: {
                'data-controller': 'autocomplete-product',
                'data-role': 'form-autocomplete-product',
            },
        })
    }}
        {{ form_row(form_autocomplete.product) }}
        {{ form_rest(form_autocomplete) }}
    {{ form_end(form_autocomplete) }} #}

    <form data-role="form-autocomplete-product">
        <select data-controller="tom-select" multiple>
            {% for product in allProducts %}
                <option value="{{ product.id }}"
                        data-option-id="{{ product.id }}"
                        data-product-name="{{ product.name }}"
                        data-category-id="{{ product.category.id }}"
                >
                    {{ product.name }}
                </option>
            {% endfor %}
        </select>
        <button type="submit" class="btn btn-dark">Send</button>
    </form>

</div>
{% endblock %}
